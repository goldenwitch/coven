# Coven.Toys.FileScrivenerConsole

Console chat with journal persistence. Demonstrates FileScrivener for saving chat history to disk.

## Prerequisites

None (no API keys required).

## How to Run

```bash
dotnet run
```

Type messages in the console; the bot echoes them and persists all chat entries to `./data/console-chat.ndjson`.
