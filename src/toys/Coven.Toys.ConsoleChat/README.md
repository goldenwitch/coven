# Coven.Toys.ConsoleChat

Minimal console-based chat echo bot. Demonstrates the basic Coven pipeline with console I/O.

## Prerequisites

None (no API keys required).

## How to Run

```bash
dotnet run
```

Type messages in the console; the bot echoes them back.
