# Coven.Toys.ConsoleOpenAI

Console chat connected to OpenAI. Demonstrates basic agent integration without streaming.

## Prerequisites

- OpenAI API key (set in `Program.cs` or via `OPENAI_API_KEY` environment variable)

## How to Run

```bash
dotnet run
```

Type messages in the console; the OpenAI agent responds.
